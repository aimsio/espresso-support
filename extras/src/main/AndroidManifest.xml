<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  package="com.novoda.espresso">

  <uses-permission
    android:name="android.permission.WRITE_SECURE_SETTINGS"
    tools:ignore="ProtectedPermissions" />

  <application tools:ignore="GoogleAppIndexingWarning">

    <activity android:name=".EmptyActivity" android:exported="true" />

    <activity android:name=".TalkBackStateSettingActivity" android:exported="true">

      <intent-filter>
        <action android:name="com.novoda.espresso.ENABLE_TALKBACK" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>

      <intent-filter>
        <action android:name="com.novoda.espresso.DISABLE_TALKBACK" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>

    </activity>

  </application>

</manifest>
